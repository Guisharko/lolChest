import { ParticipantIdentityDto } from './participant-identity-dto';

describe('ParticipantIdentityDto', () => {
  it('should create an instance', () => {
    expect(new ParticipantIdentityDto()).toBeTruthy();
  });
});
