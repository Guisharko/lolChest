import { ParticipantTimelineDto } from './participant-timeline-dto';

describe('ParticipantTimelineDto', () => {
  it('should create an instance', () => {
    expect(new ParticipantTimelineDto()).toBeTruthy();
  });
});
